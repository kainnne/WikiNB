---
interface Props {
  placeholder?: string;
  size?: 'sm' | 'lg';
  action?: string;
  showButton?: boolean;
  buttonLabel?: string;
}

const {
  placeholder = '搜尋筆記…',
  size = 'sm',
  action = '/search',
  showButton = false,
  buttonLabel = 'Search',
} = Astro.props;

const base = import.meta.env.BASE_URL;
const formAction = action.startsWith('http') ? action : `${base}${action.replace(/^\//, '')}`;
---

<form action={formAction} method="get" class="w-full">
  <div class="relative w-full">
    <input
      type="search"
      name="q"
      placeholder={placeholder}
      class:list={[
        'search-input',
        size === 'lg' && 'py-4 text-lg',
      ]}
      autocomplete="off"
    />
  </div>
  {showButton && (
    <button
      type="submit"
      class="btn-primary mt-4 w-full justify-center py-3 text-base md:text-lg"
    >
      {buttonLabel}
    </button>
  )}
</form>
